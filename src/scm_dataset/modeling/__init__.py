"""Classical GraphSAGE modeling stage (CLASSICAL_GRAPHSAGE_IMPLEMENTATION_PLAN.md).

Sits on top of the already-complete benchmark under `data/benchmark/`
(see `..benchmark`, `..export`, `..labels`). This package never regenerates
or mutates that benchmark -- it only reads it.
"""

from __future__ import annotations
